input{
    jdbc{
        jdbc_driver_library => "/usr/share/logstash/mysql-connector-java-8.0.28.jar"
        jdbc_driver_class => "com.mysql.cj.jdbc.Driver"
        jdbc_connection_string => "jdbc:mysql://my-database:3306/teamcloud04" # 연결할 DB 정보
        jdbc_user => "root"
        jdbc_password => "3198"
        schedule =>  "*/20 * * * * *"
        use_column_value => "true"
        tracking_column => "updatedat"
        tracking_column_type => "numeric"
        last_run_metadata_path => "./cafeList.txt"
        # statement => "select id, title, address, contents, unix_timestamp(updatedat) as updatedat from cafelist where (unix_timestamp(updatedat) > :sql_last_value AND updatedat < NOW()) order by updatedat asc" # 명령어 작성 (SQL문 사용)"
        statement => "select id, title, contents, unix_timestamp(updatedat) as updatedat from cafelist where (unix_timestamp(updatedat) > :sql_last_value AND updatedat < NOW()) order by updatedat asc" # 명령어 작성 (SQL문 사용)  
    }
}

filter{
    mutate {
        copy => {"id" => "[@metadata][_id]"}
        remove_field => ["id", "@version", "@timestamp", "updatedat"]
    }
}

output {
    elasticsearch {
        hosts => "elasticsearch:9200"
        index => "search-cafelist" # 저장할 테이블명
        template => "/usr/share/logstash/cafe.json."
        template_name => "cafe"
        manage_template => true
        template_overwrite => true
    }
}